<footer>

<div class="container">
    {{#each data.footer.links}}
    <a target="_blank" href="{{link}}">{{text}}</a>

    {{#unless @last}}
    |
    {{/unless}}

    {{/each}}

    </div>
    
</footer>