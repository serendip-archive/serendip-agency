<footer  data-aos-duration="500" data-aos="fade-in" data-aos-delay="500">

<div class="container">
    {{#each data.footer.links}}
    <a target="_blank" href="{{link}}">{{text}}</a>

    {{#unless @last}}
    |
    {{/unless}}

    {{/each}}

    </div>
    
</footer>